[project]
name = "ns2audio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "neo>=0.13.4",
    "scipy>=1.14.1",
    "typer>=0.15.1",
]

[project.optional-dependencies]
dev = [
    "matplotlib>=3.9.3",
]
